# E8 — Cloudflare Compute

Question: does the current Workers AI catalog expose the paper's exact base model plus arbitrary customer adapter loading?

```bash
bash probe.sh
```

Pass requires both. Catalog LoRA variants are not evidence of an arbitrary adapter upload/select API.
