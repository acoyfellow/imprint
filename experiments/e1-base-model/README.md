# E1 — Base Model

Question: can the paper's exact base model run one deterministic completion on this Mac?

```bash
uv run run.py
```

Pass: `Qwen/Qwen2.5-Coder-1.5B` loads, records its resolved revision, and completes the assertion with `42`.
