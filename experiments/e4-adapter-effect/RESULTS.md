# E4 result

**Pass.** The generated adapter loaded into `Qwen2.5-Coder-1.5B` and inference completed with no repository source in the prompt.

The neutral smoke output changed from a verbose `42` answer to `42`. This proves adapter effect, not repository-task quality. See `receipt.json`.
