# Contributing

1. Open an issue describing the behavior to change.
2. Add an executable proof or regression test.
3. Run `bun run typecheck`, `bun run lint`, and `bun test`.
4. Keep the public API small and commit-bound.

Imprint stays at `0.0.1`.
